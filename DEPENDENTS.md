# Dependents

Projects that depend on **tessara**. Update them after changes to the public API,
entry points, or package metadata.

| Project | Path | Mechanism |
|---------|------|-----------|
| RS-ctx-dep-net | `work/research/RS-ctx-dep-net` | editable pip install (`environment.yml`) |
